// @flow

import React from "react";

const SkipLinks = () => (
  <div className="skip-links">
    <a href="#onePunchMan">Jump to One punch man</a>
    <a href="#onePiece">Jump to One piece</a>
    <a href="#deathNote">Jump to Death Note</a>
  </div>
);

export default SkipLinks;
